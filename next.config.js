/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['xsgames.co', 'flowbite.s3.amazonaws.com', 'flowbite.com', 'images.unsplash.com'],
    }
}

module.exports = nextConfig
