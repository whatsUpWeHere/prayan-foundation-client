/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['xsgames.co', 'flowbite.s3.amazonaws.com', 'flowbite.com', 'images.unsplash.com', 'images.clerk.dev'],
    }
}

module.exports = nextConfig
